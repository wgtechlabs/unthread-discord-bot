# =============================================================================
# UNTHREAD DISCORD BOT - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your actual values
# Only required variables are listed here. All other values use sensible defaults.
# =============================================================================

# ======= Discord Bot Configuration (Required) =======
# Discord Bot Token
# How to get: Discord Developer Portal → Applications → Your App → Bot → Token
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Discord Application ID (Client ID)
# How to get: Discord Developer Portal → Applications → Your App → General Information → Application ID
CLIENT_ID=your_client_id_here

# Discord Guild ID (Server ID)
# How to get: Enable Developer Mode in Discord → Right-click your server → Copy ID
GUILD_ID=your_guild_id_here

# ======= Unthread Integration (Required) =======
# Unthread API Key
# How to get: Login to Unthread dashboard → Settings → API Keys → Generate new key
UNTHREAD_API_KEY=your_unthread_api_key_here

# Unthread Slack Channel ID
# How to get: Unthread dashboard → Settings → Integrations → Slack → Channel ID
UNTHREAD_SLACK_CHANNEL_ID=your_unthread_slack_channel_id_here

# Unthread Webhook Secret
# How to get: Unthread dashboard → Settings → Webhooks → Create webhook → Copy secret
UNTHREAD_WEBHOOK_SECRET=your_unthread_webhook_secret_here

# ======= Infrastructure (Required) =======
# PostgreSQL Database URL
# Docker (default): postgresql://postgres:postgres@postgres-platform:5432/unthread_discord_bot
# Local development: postgresql://postgres:postgres@localhost:5432/unthread_discord_bot
# Railway: Uses Railway's PostgreSQL service
POSTGRES_URL=postgresql://user:pass@host:port/db

# Platform Redis URL (for bot state and caching)
# Docker (default): redis://redis-platform:6379
# Local development: redis://localhost:6379
# Railway: Uses Railway's Redis service
PLATFORM_REDIS_URL=redis://redis-platform:6379

# Webhook Redis URL (for webhook message queuing)
# Docker (default): redis://redis-webhook:6379
# Local development: redis://localhost:6380
# Railway: Uses Railway's Redis service for webhook processing
WEBHOOK_REDIS_URL=redis://redis-webhook:6379

# ======= Discord Configuration (Required) =======
# ======= Discord Configuration (Required) =======
# Forum Channel IDs (comma-separated list for auto-ticket creation)
# How to get: Enable Developer Mode → Right-click forum channel → Copy ID
FORUM_CHANNEL_IDS=channel_id_1,channel_id_2

# ======= Optional Development Override =======
# Node Environment (only set for development)
# Defaults to 'production' for production-safe logging
# NODE_ENV=development

# ======= Optional Configuration =======
# Slack Workspace ID (only needed for file attachments)
# How to get: Slack workspace URL or from Slack API, format: T + 10 characters
# SLACK_TEAM_ID=your_slack_team_id

# =============================================================================
# ALL OTHER CONFIGURATION USES SENSIBLE DEFAULTS
# =============================================================================
# The following are handled automatically by src/config/defaults.ts:
# - PORT: 3000
# - UNTHREAD_HTTP_TIMEOUT_MS: 10000
# - WEBHOOK_POLL_INTERVAL: 1000
# - UNTHREAD_DEFAULT_PRIORITY: 5
# - DUMMY_EMAIL_DOMAIN: discord.invalid
# - DATABASE_SSL_VALIDATE: true
#
# You can override any of these by uncommenting and setting values above.
# =============================================================================